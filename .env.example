# Amaejozu-frontend/.env.local
# Copy this file to .env.local and fill in your values

# API URL for browser requests (client-side)
API_URL=http://localhost:8000

# API URL for server-side requests (Server Actions, Route Handlers)
# Use internal Docker network URL in production (e.g., http://backend:8000)
INTERNAL_API_URL=http://localhost:8000
